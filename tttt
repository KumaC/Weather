define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var Base.abc = NC.Base;
    var Clazz.123 = NC.Clazz;
    var Componentttt = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});define(function (require, exports, module) {
    var NC = require('nc');
    var CPN = require('cpn');

    var $ = NC.$;
    var Base = NC.Base;
    var Clazz = NC.Clazz;
    var Component = NC.Component;

    var Cpn = module.exports = Clazz.create();

    Clazz.mix(Cpn, Component, {
        _template: [

        ].join(''),
        listeners: [{
            name: 'render',
            type: 'custom',
            handler: function () {
                var that = this;

            }
        }]
    }, {

    });
    
});
